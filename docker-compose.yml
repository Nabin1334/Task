services:
  backend:
    build: ./backend
    ports: ["8000:8000"]
    environment: ["REDIS_URL=redis://redis:6379/0"]
  frontend:
    build: ./frontend
    ports: ["5173:80"]
  redis:
    image: redis:7-alpine