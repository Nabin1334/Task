# Orbit Tasks

A small task management application with a Django API, JWT authentication, Redis cache hooks, and a React/Vite dashboard.

## Run locally

Backend: `cd backend`, `pip install -r requirements.txt`, `python manage.py migrate`, `python manage.py runserver`.

Frontend: `cd frontend`, `npm install`, `npm run dev`.

Or run the full stack with `docker compose up --build`.
